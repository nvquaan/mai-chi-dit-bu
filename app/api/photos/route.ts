
// File này đã được gỡ bỏ vì ứng dụng đang chạy ở môi trường Client-side (SPA).
// Việc gọi API nội bộ sẽ gây lỗi 404. 
// Vui lòng sử dụng logic lấy ảnh theo Tag trong services/cloudinaryService.ts.
export {};
